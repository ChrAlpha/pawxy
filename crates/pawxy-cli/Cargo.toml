[package]
name = "pawxy-cli"
version = "0.2.0"
edition.workspace = true
license.workspace = true

[dependencies]
pawxy-core = { path = "../pawxy-core" }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal"] }
